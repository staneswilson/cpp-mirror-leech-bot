# ---------------------------------------------------------------------------
# tests/unit/infrastructure/media/CMakeLists.txt
#
# Intentionally empty placeholder. The media processors (ffmpeg / 7-zip) are external-tool wrappers;
# meaningful coverage requires real binaries on PATH, so the tests live under tests/integration/
# instead.
# ---------------------------------------------------------------------------
